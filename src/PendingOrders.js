import React, { Component } from 'react';

class PendingOrders extends Component {
 render() {
  return (
   <div>
        View for displaying all the pending orders. Try make it stateless functional component and have all the orders
        passed down from App component
   </div>
  );
 }
}

export default PendingOrders;